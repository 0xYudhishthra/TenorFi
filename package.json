{
  "name": "keel",
  "version": "0.0.0",
  "private": true,
  "description": "On-chain fixed-funding-rate swaps, built natively on 1inch Aqua.",
  "license": "MIT",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "contracts:build": "pnpm --filter @keel/contracts build",
    "contracts:test": "pnpm --filter @keel/contracts test"
  }
}
